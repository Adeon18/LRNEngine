#pragma once

// This is the "config" file for the project that contains useful defines to tell which project version to run
// For example here we can choose whether we want to run a raytracer or a DX11 Engine

#define DX_ENGINE 0